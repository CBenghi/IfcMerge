using System.IO;

namespace IfcMerge
{
    public class IfcMerger
    {
        internal int processed = 0;

        internal void MergeFile(FileInfo f)
        {
            return;
        }

        internal FileInfo SaveIfcModel(Options opts)
        {
            return new FileInfo("");
        }
    }
}
